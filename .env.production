# Production environment variables for Vercel
# Use Vercel serverless function proxy to avoid Mixed Content issues
# Empty string so that /api/login becomes /api/login (not /api/api/login)
VITE_API_URL=
